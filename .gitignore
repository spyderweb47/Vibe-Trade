node_modules/
.next/
__pycache__/
*.pyc
.env
.venv/
venv/
dist/
build/
*.egg-info/
.turbo/

# Generated frontend bundle — built by `vibe-trade build-frontend`.
# Gitignored because it's an artifact of the Next.js build; ships inside
# release wheels via `python -m build` which runs the build as a pre-step.
apps/web/out/
vibe_trade/web_static/
