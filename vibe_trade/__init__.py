"""
Vibe Trade — AI-powered trading agent platform CLI.

Installable via `pipx install vibe-trade` (or `pip install vibe-trade`). Once
installed, a `vibe-trade` command is available globally with subcommands:

    vibe-trade serve                 # start the web UI + backend
    vibe-trade fetch BTC/USDT 1h     # download market data
    vibe-trade simulate              # run a multi-agent debate
    vibe-trade skills list           # list registered skills
    vibe-trade version               # print the installed version
"""

__version__ = "0.2.2"
